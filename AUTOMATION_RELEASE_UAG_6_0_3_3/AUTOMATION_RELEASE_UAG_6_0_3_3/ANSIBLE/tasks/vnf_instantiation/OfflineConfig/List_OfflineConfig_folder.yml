---

      - include_tasks : ../find_files.yml
        vars:
            FILE_PATH: "{{PATH}}"
            FILE_PATTERN: "*VNFD*.xml"

      - name: build array of OFFLINECONFIG_ADD_VNFD_PATHS
        set_fact: OFFLINECONFIG_ADD_VNFD_PATHS="{{OFFLINECONFIG_ADD_VNFD_PATHS}}+['{{fileitem.path}}']"
        with_items: "{{files_found.files}}"
        loop_control:
            loop_var: fileitem
