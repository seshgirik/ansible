---

      - include_tasks : ../find_files.yml
                            FILE_PATH="{{XML_PATH}}"
                            FILE_PATTERN="{{XML_PATTERN}}"

      - name: build array of DESCRIPTOR_ONBOARD_PATHS
        set_fact: DESCRIPTOR_ONBOARD_PATHS="{{DESCRIPTOR_ONBOARD_PATHS}}+['{{fileitem.path}}']"
        with_items: "{{files_found.files}}"
        loop_control:
            loop_var: fileitem



#      - name: loop over result
#        debug:
#          msg="{{XML_PATTERN}}{{item.path}}"
#        with_items: "{{ files_found.files }}"
